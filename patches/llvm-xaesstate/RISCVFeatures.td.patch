--- a/llvm/lib/Target/RISCV/RISCVFeatures.td
+++ b/llvm/lib/Target/RISCV/RISCVFeatures.td
@@ -1324,6 +1324,17 @@
       AssemblerPredicate<(all_of FeatureVendorXAesKeyExp),
                          "'XAesKeyExp' (custom AES key schedule)">;
 
+// PDP project-10 custom extension - round-wise AES-128 state accelerator
+// (hidden 128-bit cipher state register advanced one round per cycle via
+// load / round / read instructions; AddRoundKey reads the live round key
+// from the adjacent XAesKeyExp register).
+def FeatureVendorXAesState
+    : RISCVExtension<1, 0, "Round-wise AES-128 state accelerator (PDP project-10)">;
+def HasVendorXAesState
+    : Predicate<"Subtarget->hasVendorXAesState()">,
+      AssemblerPredicate<(all_of FeatureVendorXAesState),
+                         "'XAesState' (custom round-wise AES)">;
+
 // MIPS Extensions
 
 def FeatureVendorXMIPSCMov
